// importamos la funcion que vamos a testear
// import {  } from '../src/lib/fireFunctions'

/* describe('myFunction', () => {
  it('debería ser una función', () => {
    expect(typeof myFunction).toBe('function')
  })
}) */
